// Write the code, one line for each action:

// Create an empty object user.
// Add the property name with a string value.
// Add the property surname with a string value.
// Add the property like with a boolean value
var user = {
    name: "Edmund",
    surname: "Pang",
    like: false,
};

// console.log(`${user.name} ${user.surname} ${user.like ? "likes" : "doesn't like"} SUTD`)